function getFirstSelector(selector){
  const match = document.querySelector(selector);
  return match;
}

function nestedTarget(){
  var nest = document.getElementById('nested')
  var result = nest.querySelector(div.target);
}